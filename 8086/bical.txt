	.MODEL SMALL
	.STACK 64
	.DATA

	DATA1 LABEL BYTE
	MAXL1 DB 20
	ACTL1 DB ?
	NUM1 DB 20 DUP(' ')

	DATA2 LABEL BYTE
	MAXL2 DB 20
	ACTL2 DB ?
	NUM2 DB 20 DUP(' ')

	.CODE

	MAIN PROC FAR

	MOV AX,@DATA
	MOV DS,AX

	MOV AL,30H
	LEA DI,NUM1
	MOV [DI],AL
	MOV AL,0DH
	MOV [DI+1],AL

	MOV AH,02H
	MOV DL,ACTL1
	INT 21H

	MOV AH,4CH
	INT 21H

MAIN ENDP
END MAIN